<Window x:Class="UIApplication.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIApplication.Views.Windows"
        xmlns:controls="clr-namespace:UIApplication.Views.Controls"
        mc:Ignorable="d"
        Name="mainWindow"
        WindowStartupLocation="CenterScreen"
        Height="720" Width="1280"
        MinHeight="720" MinWidth="1280"
        Loaded="mainWindow_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <controls:SideBarControl
            Grid.Column="0"
            x:Name="sideBarControl"/>
        <Button
            Grid.Column="1"
            Name="whiteButton"
            Style="{StaticResource WhiteButton}"
            Click="whiteButton_Click">
            <TextBlock
                Text="Click me"
                Style="{StaticResource ButtonTextBlock}"/>
        </Button>
    </Grid>
</Window>
